name: scoring_engine
description: Pure Dart scoring engine for number kubb (Scandinavian kegelspel). No Flutter dependencies.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.12.0

dependencies:
  meta: ^1.15.0

dev_dependencies:
  test: ^1.25.0
